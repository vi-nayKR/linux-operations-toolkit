# Curated Evidence

No runtime claim is recorded here until the public workflow completes successfully. CI artifacts are intentionally short-lived and exclude SSH private keys and backup contents.

Expected acceptance fields are defined in [../docs/EVIDENCE.md](../docs/EVIDENCE.md).
