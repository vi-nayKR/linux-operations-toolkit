---
- name: Converge the Linux reliability baseline
  hosts: linux_fleet
  become: true
  gather_facts: true
  roles:
    - role: linux_baseline
    - role: durable_backup
    - role: safe_diagnostics
